# Port the Express API listens on (set via PM2 env block in production)
PORT=3003

# Path to the SQLite database file. Defaults to <server dir>/data.db
# DB_PATH=/var/www/portfolio/apps/thejcrew/server/data.db
